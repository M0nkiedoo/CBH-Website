/* Container for the image */
.image-container {
  position: relative;
  display: inline-block;
  width: 100%;
  height: auto;
}

.image-container img {
  width: 100%;
  height: auto;
}

/* Title and subtitle in the middle of the image */
.text-overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%); /* Centers it */
  text-align: center;
  color: white;
  background: rgba(0, 0, 0, 0.5); /* Slight background for readability */
  padding: 20px;
  border-radius: 10px;
}

.text-overlay h1 {
  font-size: 3rem;
  margin: 0;
}

.text-overlay p {
  font-size: 1.5rem;
  margin: 10px 0 20px;
}

/* Button Styles */
.button-link {
  display: inline-block;
  padding: 15px 25px;
  background-color: #2b2b2b;
  color: white;
  text-decoration: none;
  font-weight: bold;
  text-transform: uppercase;
  border-radius: 5px;
  transition: background-color 0.3s ease;
  margin-top: 20px;
}

.button-link:hover {
  background-color: red; /* Red color when hovering or clicked */
}

/* Optional: Add a smooth scroll to anchor */
html {
  scroll-behavior: smooth;
}
