/* Container for the image */
.image-container {
  position: relative;
  display: inline-block;
  width: 100%;
  height: 80vh;  /* Limit the height to 80% of the viewport height */
  overflow: hidden; /* Prevent the image from overflowing */
}

.image-container img {
  width: 100%;
  height: 100%;
  object-fit: cover;  /* Ensures the image covers the container without stretching */
}

/* Title and subtitle in the middle of the image */
.text-overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%); /* This centers it */
  text-align: center;
  color: white;
  background: rgba(0, 0, 0, 0.5); /* Background for readability */
  padding: 20px;
  border-radius: 10px;
  max-width: 90%;  /* Ensure the text doesn't stretch too wide */
}

.text-overlay h1 {
  font-size: 2.5rem; /* Adjusted for better size */
  margin: 0;
}

.text-overlay p {
  font-size: 1.25rem; /* Adjusted for better size */
  margin: 10px 0 20px;
}

/* Button Styles */
.button-link {
  display: inline-block;
  padding: 15px 25px;
  background-color: #2b2b2b;
  color: white;
  text-decoration: none;
  font-weight: bold;
  text-transform: uppercase;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.button-link:hover {
  background-color: red; /* Button turns red when hovered or clicked */
}

/* Optional: Add smooth scrolling when navigating to an anchor */
html {
  scroll-behavior: smooth;
}
